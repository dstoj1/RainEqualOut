 @model List<RainEqualsOut.Models.Inventory>
@{
    ViewBag.Title = "Products";
}
<h2>@ViewBag.Title.</h2>
@*@Html.ActionLink("Add Pick Up", "AddAddress", "Customer")*@
@if (Model.Count > 0)
{
    <table class="table table-bordered table-hover">
        <thead>
            <tr>
                <th>Products Availabl</th>
            </tr>
        </thead>
        <tbody>
            @foreach (RainEqualsOut.Models.Inventory inventory in Model)
                {
                <tr>
                    <td>
                        @inventory.Photo<br />  @inventory.Product<br />  @inventory.Description<br />  @inventory.Size<br /> @inventory.Color<br /> @inventory.Type<br /> $@inventory.Price <br />
                        @Html.ActionLink("Remove Vendor", "RemoveVendor", "Vendor", new { InventoryId = inventory.ID }, null)<br />
                        @Html.ActionLink("Edit Vendor Info", "Edit", "Vendor", new { InventoryId = inventory.ID }, null)
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p> No Product found,  Please Enter Product Information</p>
}